class Request {

  String title;
  String artist;
  DateTime date;
  String period;
  String url;
  String id;
  bool isPlayed;
  bool isDeleted;

  Request({ this.title, this.artist, this.date, this.period, this.url,  this.id, this.isPlayed, this.isDeleted});
}